/*
🔹  JavaScript Variable — কোনটা কখন ব্যবহার করবো?
1.  var (পুরানো ধরণ)
    1.  JavaScript এর প্রথম দিককার ভ্যারিয়েবল।
    2.  Function Scope হয়, কিন্তু Block Scope মানে { } এর ভিতরে কাজ করে না।
    3.  Hoisting এর কারণে অনেক সময় বিভ্রান্তি তৈরি করে।
👉 উদাহরণ:
var x = 10;
if (true) {
    var x = 20; 
    console.log(x); // 20
}
console.log(x); // 20 (বাইরের ভ্যারিয়েবলও পরিবর্তন হয়ে গেছে!)
⚠️ তাই var এখন আর ব্যবহার করা ভালো না। শুধু পুরানো কোডে দেখবে।

2.  let (modern ব্যবহারযোগ্য)
    1.  Block Scope (মানে { } এর ভিতরে সীমাবদ্ধ থাকবে)।
    2.  মান (value) পরিবর্তন করা যায়।
    3.  Hoisting হয় না, তাই নিরাপদ।
👉 উদাহরণ:
let y = 10;
if (true) {
    let y = 20; 
    console.log(y); // 20 (শুধু ব্লকের ভিতরে পরিবর্তন)
}
console.log(y); // 10 (বাইরের মান একই থাকে)
✅ যখন ভ্যারিয়েবলের মান পরিবর্তন করতে হবে → let ব্যবহার করো।

3.  const (স্থায়ী মানের জন্য)
    1.  Block Scope।
    2.  একবার মান দিলে আবার পরিবর্তন করা যায় না।
    3.  সবসময় মান থাকতে হবে (empty রাখা যাবে না)।
👉 উদাহরণ:
const pi = 3.1416;
// pi = 3.15; ❌ Error, কারণ const পরিবর্তন করা যাবে না
console.log(pi);

👉 তবে যদি Object বা Array হয়, তখন তার ভেতরের মান পরিবর্তন করা যায়।
const person = { name: "Kadir", age: 22 };
person.age = 23; // ভিতরের মান পরিবর্তন করা যাবে
console.log(person); // { name: "Kadir", age: 23 }
✅ যখন ভ্যারিয়েবল পরিবর্তন করা লাগবে না (স্থায়ী রাখবে) → const ব্যবহার করো।

📝  সহজ নিয়ম (Shortcut)
    1.  const → সবসময় প্রথমে const ব্যবহার করো।
    2.  যদি মান পরিবর্তন করার দরকার পড়ে → let ব্যবহার করো।
    3.  var → ব্যবহার না করাই ভালো (শুধু পুরানো কোড বুঝতে কাজে লাগে)।

🎯 সারসংক্ষেপ
    1.  var = পুরানো, function scope, এড়ানো ভালো।
    2.  let = পরিবর্তনশীল ভ্যারিয়েবল, block scope।
    3.  const = স্থায়ী ভ্যারিয়েবল, block scope।
*/