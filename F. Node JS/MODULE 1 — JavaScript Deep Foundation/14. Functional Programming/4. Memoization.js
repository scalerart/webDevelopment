/*
4.  Memoization

Memoization হলো এমন একটি অপ্টিমাইজেশন কৌশল যার মাধ্যমে একটি ফাংশনের ব্যয়বহুল ক্যালকুলেশনের ফলাফলকে মেমরিতে ক্যাশ (Cache) করে রাখা হয়। যখন একই ইনপুট দিয়ে ফাংশনটি আবার কল করা হয়, তখন পুনরায় হিসাব না করে মেমরি থেকে সরাসরি ফলাফলটি রিটার্ন করা হয়।

১.  সংজ্ঞা
    এটি মূলত একটি 'Caching' কৌশল। এর প্রধান লক্ষ্য হলো একই ইনপুটের জন্য বারবার ভারী ক্যালকুলেশন বা ভারী অপারেশন না করে দ্রুত আউটপুট দেওয়া। এটি Pure Function-এর ক্ষেত্রে সবচেয়ে ভালো কাজ করে।

২.  কোড উদাহরণ
    সাধারণ ফাংশন (যেটি ধীর):

    JavaScript
        function heavyCalculation(num) {
            // ধরুন এটি একটি জটিল গাণিতিক অপারেশন
            for (let i = 0; i < 1000000000; i++) {} 
            return num * 2;
        }

    Memoized ফাংশন (যেটি দ্রুত):

    JavaScript
        const cache = {}; // ফলাফল জমিয়ে রাখার জায়গা

        function memoizedCalculation(num) {
            if (num in cache) {
                console.log("ক্যাশ থেকে নিয়েছি!");
                return cache[num];
            } else {
                console.log("নতুন ক্যালকুলেশন করছি...");
                let result = num * 2; // ভারী ক্যালকুলেশন
                cache[num] = result;
                return result;
            }
        }

৩.  ইন্টারভিউতে মনে রাখার মতো পয়েন্ট (Key Takeaways)
    কখন মেমাইজেশন ব্যবহার করবেন?
        ১.  যদি ফাংশনটি Pure Function হয়।
        ২.  যদি ফাংশনটি ভারী ক্যালকুলেশন করে।
        ৩.  যদি ফাংশনটি একই ইনপুট নিয়ে বারবার কল করা হয়।

    ট্রেড-অফ (Trade-off): মেমাইজেশন সময় বাঁচায় (Speed), কিন্তু মেমরি খরচ বাড়ায় (Space)। তাই খুব বেশি ডাটা ক্যাশ করলে মেমরি লিকেজের ঝুঁকি থাকে।

৪.  ইন্টারভিউ টিপস
    ইন্টারভিউয়ার জিজ্ঞেস করতে পারেন— "মেমাইজেশন কি সব ধরনের ফাংশনের জন্য ভালো?"

    উত্তর: "না। যদি কোনো ফাংশন খুব দ্রুত কাজ করে, তবে মেমাইজেশন ব্যবহার করা সময় ও মেমরির অপচয়। আবার যদি ফাংশনটি 'Impure' হয় (অর্থাৎ যার আউটপুট বাইরের অবস্থার ওপর নির্ভর করে), তবে মেমাইজেশন ব্যবহার করলে ভুল রেজাল্ট আসার সম্ভাবনা থাকে। তাই এটি ব্যবহারের আগে ফাংশনটির প্রকৃতি বুঝে নেওয়া জরুরি।"
*/