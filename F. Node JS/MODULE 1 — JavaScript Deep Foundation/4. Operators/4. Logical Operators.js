/*
4.  Logical Operators

১.  Logical Operators (লজিক্যাল অপারেটর)
    সংজ্ঞা: Logical Operators ব্যবহার করা হয় একাধিক কন্ডিশন বা শর্তকে একত্রে যাচাই করার জন্য। এগুলো মূলত লজিক্যাল সিদ্ধান্ত (Decision making) নিতে সাহায্য করে এবং সব সময় একটি Boolean (true বা false) মান রিটার্ন করে।

২.  মূল অপারেটরসমূহ:
    ১.  && = AND (Logical AND): সব কন্ডিশন true হলে তবেই true রিটার্ন করে।
    ২.  || = OR (Logial OR): একটি কন্ডিশন true হলে তবেই true রিটার্ন করে।
    ৩.  ! = NOT (Logical NOT): মানটিকে উল্টে দেয় (true হলে false, false হলে true)।

৩.  বিস্তারিত ব্যাখ্যা
    ১.  && (AND):
        এটি খুবই কঠোর। যদি একাধিক শর্তের মধ্যে একটিও false হয়, তবে পুরো এক্সপ্রেশনটি false হয়ে যায়।
        উদাহরণ: (বয়স >= ১৮ && আইডি_কার্ড == true) — এখানে দুটোই সত্য হতে হবে।

    ২.  || (OR): এটি কিছুটা নমনীয়। যেকোনো একটি সত্য হলেই এটি true রিটার্ন করে।
        উদাহরণ: (আছে_টাকা || আছে_ক্রেডিট_কার্ড) — যেকোনো একটি থাকলেই কেনাকাটা সম্ভব।

    ৩.  ! (NOT): এটি ভেরিয়েবলের বর্তমান অবস্থার বিপরীত মান প্রদান করে।
        উদাহরণ: !true করলে ফলাফল আসবে false।

৪.  ইন্টারভিউয়ের জন্য গুরুত্বপূর্ণ কনসেপ্ট: Short-Circuiting
    ইন্টারভিউতে এটি মাস্ট-আস্ক প্রশ্ন। জাভাস্ক্রিপ্টে এই অপারেটরগুলো Short-circuit evaluation অনুসরণ করে:

    ১.  && (AND) এর ক্ষেত্রে:
        যদি প্রথম কন্ডিশনটি false হয়, তবে জাভাস্ক্রিপ্ট পরের কন্ডিশনটি আর চেকই করে না (কারণ ফলাফল তো false হবেই)।

    ২.  || (OR) এর ক্ষেত্রে:
        যদি প্রথম কন্ডিশনটি true হয়, তবে জাভাস্ক্রিপ্ট পরের কন্ডিশনটি আর চেক করে না (কারণ ফলাফল true হবেই)।

    প্রো-টিপ: Node.js-এ আমরা প্রায়ই ডিফল্ট ভ্যালু সেট করার জন্য এটি ব্যবহার করি। যেমন: const port = process.env.PORT || 3000; (যদি PORT ভেরিয়েবলটি না থাকে, তবে ডিফল্ট হিসেবে ৩০০০ ব্যবহার হবে)।

৫.  কোড উদাহরণ
    JavaScript
        let isLogged = true;
        let hasPermission = false;

        console.log(isLogged && hasPermission); // false (কারণ একটি false)
        console.log(isLogged || hasPermission); // true (কারণ একটি true)
        console.log(!isLogged);                // false

সহজ মনে রাখার উপায়: && মানে হলো "এবং" (সব হতে হবে), || মানে হলো "অথবা" (যেকোনো একটা হলেই হবে), আর ! মানে হলো "না" (যা আছে তার উল্টো)।        
*/