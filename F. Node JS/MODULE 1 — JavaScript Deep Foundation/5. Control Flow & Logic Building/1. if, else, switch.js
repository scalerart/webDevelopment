/*
1.  if, else, switch

১.  if...else Statement
    এটি সবচেয়ে সাধারণ উপায়। যখন একটি নির্দিষ্ট শর্ত সত্য (true) হয়, তখন একটি কোড ব্লক রান করবে, অন্যথায় অন্য আরেকটি ব্লক রান করবে।

    JavaScript
        let score = 85;

        if (score >= 90) {
            console.log("Grade: A");
        } else if (score >= 80) {
            console.log("Grade: B");
        } else {
            console.log("Grade: F");
        }

    ১.  if: প্রথম শর্ত যাচাই করে।
    ২.  else if: একাধিক শর্ত যাচাই করতে ব্যবহার হয়।
    ৩.  else: সব শর্ত মিথ্যা হলে এটি রান করে।~

২.  switch Statement
    যখন আপনার কাছে একটি ভেরিয়েবলের অনেকগুলো নির্দিষ্ট মান (value) যাচাই করার প্রয়োজন হয়, তখন switch অনেক বেশি গোছালো।

    JavaScript
        let day = "Monday";

        switch (day) {
            case "Saturday":
                console.log("আজ ছুটির দিন!");
                break;
            case "Monday":
                console.log("আজ কাজের দিন।");
                break;
            default:
                console.log("সাধারণ দিন।");
        }

    ১.  case: ভেরিয়েবলটির মান যদি এর সাথে মেলে, তবে ওই কোডটি রান করবে।

    ২.  break:
        এটি খুবই গুরুত্বপূর্ণ। break না থাকলে জাভাস্ক্রিপ্ট পরের সব কেসগুলোও রান করে দেবে (একে বলা হয় Fall-through)।

    ৩.  default: কোনো কেসই না মিললে এটি রান করে।

৩.  ইন্টারভিউতে মনে রাখার মতো পয়েন্ট (Key Takeaways)
    ১.  if/else বনাম switch:
        if/else ব্যবহার করুন যখন শর্তগুলো জটিল হয় (যেমন: x > 10 && x < 20)।
        switch ব্যবহার করুন যখন আপনার কাছে নির্দিষ্ট কিছু মান আছে (যেমন: মেনু অপশন বা দিনের নাম)।

    ২.  Strict Comparison:
        switch সব সময় Strict Equality (===) চেক করে। অর্থাৎ, যদি ভ্যালু ১ (number) হয় আর কেস ১ (string) হয়, তবে সেটি মিলবে না।

৪.  ইন্টারভিউ টিপস
    ইন্টারভিউয়ার জিজ্ঞেস করতে পারেন— "সুইচ স্টেটমেন্টে break কেন দিতে হয়?"

    উত্তর: "জাভাস্ক্রিপ্টে সুইচ স্টেটমেন্টের ডিফল্ট বৈশিষ্ট্য হলো 'Fall-through'। অর্থাৎ একটি কেস ম্যাচ করলে জাভাস্ক্রিপ্ট পরের কেসগুলোও চেক না করেই এক্সিকিউট করতে থাকে। break ব্যবহার করে আমরা সেই চেইনটি ভেঙে দেই এবং শুধুমাত্র নির্দিষ্ট কোডটিই রান করি।"
*/