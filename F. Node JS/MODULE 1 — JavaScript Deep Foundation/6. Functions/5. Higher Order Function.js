/*
5.  Higher Order Function

১.  Higher-Order Functions (হায়ার-অর্ডার ফাংশন)
    সংজ্ঞা: যে ফাংশন অন্য কোনো ফাংশনকে আর্গুমেন্ট হিসেবে গ্রহণ করে অথবা আউটপুট হিসেবে একটি ফাংশন রিটার্ন করে, তাকেই Higher-Order Function বলা হয়।

    জাভাস্ক্রিপ্ট একটি "Functional Programming" ভাষা, আর এই হায়ার-অর্ডার ফাংশনগুলোই এর মূল শক্তি।

২.  কিভাবে কাজ করে?
    ১.  ফাংশনকে আর্গুমেন্ট হিসেবে গ্রহণ করা (Callback)
        এটি সবচেয়ে সাধারণ ব্যবহার। যেমন map(), filter(), reduce()—এগুলো সবই হায়ার-অর্ডার ফাংশন কারণ এদের ভেতর আমরা আরেকটা ফাংশন পাঠিয়ে দিই।

        JavaScript
            function greet(name) {
                return "Hello, " + name;
            }

            // এটি একটি Higher-Order Function
            function processUser(userName, fn) {
                console.log(fn(userName));
            }

            processUser("Rahim", greet); // আউটপুট: "Hello, Rahim"

    ২.  ফাংশন রিটার্ন করা
        একটি ফাংশন থেকে আরেকটি ফাংশন তৈরি করে পাঠানো।
        JavaScript
            function multiplier(factor) {
                return function(number) {
                    return number * factor;
                };
            }

            const double = multiplier(2);
            console.log(double(5)); // আউটপুট: 10

৩.  কেন এটি গুরুত্বপূর্ণ?
    ১.  কোড পুনঃব্যবহারযোগ্যতা (Reusability):
        বারবার একই কোড না লিখে লজিক আলাদা করে ফেলা যায়।

    ২.  অ্যাবস্ট্রাকশন (Abstraction):
        আমরা লুপের ভেতর কী ঘটছে তা নিয়ে চিন্তা না করে সরাসরি map বা filter ব্যবহার করে ডেটা প্রসেস করতে পারি।

    ৩.  Async Operations:
        Node.js-এর পুরো জগৎটাই চলে এই হায়ার-অর্ডার ফাংশন (Callback) এর ওপর ভিত্তি করে। যেমন ফাইল রিড করা বা সার্ভারে রিকোয়েস্ট আসার পর কী হবে—সবই Callback ফাংশনের মাধ্যমে হ্যান্ডেল হয়।

৪.  Key Points
    ১.  বিল্ট-ইন হায়ার-অর্ডার ফাংশন:
        ইন্টারভিউতে জিজ্ঞেস করতে পারে জাভাস্ক্রিপ্টের কয়েকটি হায়ার-অর্ডার ফাংশনের উদাহরণ দাও। উত্তর হবে: map(), filter(), reduce(), forEach(), find()।

    ২.  পার্থক্য:
        সাধারণ ফাংশন শুধু ডেটা (যেমন: String, Number, Object) নিয়ে কাজ করে, কিন্তু হায়ার-অর্ডার ফাংশন কোড (Function) নিয়েই কাজ করতে পারে।

সহজ মনে রাখার উপায়: যদি কোনো ফাংশন অন্য ফাংশনকে নিজের "বন্ধুর" মতো সাথে নিয়ে কাজ করে অথবা নতুন কোনো "ফাংশন" জন্ম দেয়, তবেই সে হায়ার-অর্ডার ফাংশন।        
*/