/*
1.  map

১.  map() Method
    সংজ্ঞা: map() হলো জাভাস্ক্রিপ্টের একটি হায়ার-অর্ডার ফাংশন, যা একটি অ্যারোর প্রতিটি উপাদানের (element) ওপর একটি নির্দিষ্ট ফাংশন প্রয়োগ করে এবং সেই ফলাফলগুলো নিয়ে একটি নতুন অ্যারো তৈরি করে। এটি আসল (original) অ্যারোকে পরিবর্তন (mutate) করে না।

২.  কিভাবে কাজ করে?
    map() অ্যারোর প্রতিটি এলিমেন্টের ওপর লুপ চালায় এবং আপনার দেওয়া ফাংশনটি প্রতিটি এলিমেন্টের ওপর কল করে। ফাংশনটি যা রিটার্ন করে, তা নতুন অ্যারোর সংশ্লিষ্ট পজিশনে জমা হয়। লুপ শেষে এটি একটি সম্পূর্ণ নতুন অ্যারো রিটার্ন করে যার দৈর্ঘ্য আগের অ্যারোর সমান থাকে।

৩.  কোড উদাহরণ:
    JavaScript
        const numbers = [1, 2, 3, 4];

        // প্রতিটি সংখ্যাকে দ্বিগুণ করা
        const doubled = numbers.map(num => num * 2);

        console.log(doubled); // আউটপুট: [2, 4, 6, 8]
        console.log(numbers); // আউটপুট: [1, 2, 3, 4] (আসল অ্যারো অপরিবর্তিত)

        // অবজেক্টের অ্যারো থেকে নির্দিষ্ট প্রপার্টি বের করা (Node.js/React-এ বহুল ব্যবহৃত)
        const users = [
            { id: 1, name: 'Rahim' },
            { id: 2, name: 'Karim' }
        ];
        const names = users.map(user => user.name); 

        console.log(names); // আউটপুট: ['Rahim', 'Karim']

৪.  উন্নত ফিচারসমূহ:
    ইন্ডেক্স ব্যবহার: আপনি চাইলে এলিমেন্টের সাথে তার পজিশন বা ইনডেক্সও ব্যবহার করতে পারেন।

    JavaScript
        const list = numbers.map((num, index) => `Item ${index} is ${num}`);

৫.  Key Points:
    ১.  কেন ব্যবহার করবেন?
        যখন আপনার অ্যারোর ডেটা ট্রান্সফর্ম (Transform) করে একটি নতুন অ্যারো পাওয়ার দরকার হয়, তখন map() ব্যবহার করা হয়। এটি কোডকে 'Functional Programming' স্টাইলে ক্লিন এবং রিডেবল করে তোলে।

    ২.  পার্থক্য (map vs forEach):
        ১.  map():
            একটি নতুন অ্যারো রিটার্ন করে। এটি ডেটা রূপান্তরের জন্য ব্যবহৃত হয়।

        ২.  forEach():
            কোনো নতুন অ্যারো রিটার্ন করে না (undefined রিটার্ন করে)। এটি শুধু লুপ চালিয়ে কোনো পার্শ্বপ্রতিক্রিয়া (যেমন: কনসোল লগ বা ডেটাবেজে সেভ করা) ঘটানোর জন্য ব্যবহৃত হয়।

    ৩.  ইমিউটেবিলিটি (Immutability):
        এটি ইন্টারভিউতে খুব গুরুত্বপূর্ণ। map() অরিজিনাল অ্যারোকে অক্ষত রাখে, যা আধুনিক জাভাস্ক্রিপ্ট এবং রিঅ্যাক্ট/নোড.জেএস ডেভেলপমেন্টে বাগ এড়াতে অত্যন্ত জরুরি।

সহজ মনে রাখার উপায়: map() মানে হলো "মানচিত্রায়ন"। আপনি এক সেট ডেটাকে অন্য একটি সেটে রূপান্তর করছেন। এটি আগের ডেটাকে টাচ না করে তার একটি নতুন "প্রতিরূপ" তৈরি করে।        
*/